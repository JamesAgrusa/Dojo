// conditions
#include <iostream>
using namespace std;

int main()
{
	bool A = true;
	bool B = false;

	int x = 10;
	int y = 0;

	// bool result = (A && B) || (x > y); (true)
	// bool result = (!A || B) && (x <= y); (false)
	// bool result = !((A == B) && (x == y)); (true)


	cout << "The value of the result is: " << result << endl;

	return 0;
}